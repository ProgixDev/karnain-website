# Generated and owned by their tools — formatting them here would mean editing files the repo
# explicitly forbids touching by hand (AGENTS.md), and pnpm would simply rewrite them anyway.
pnpm-lock.yaml
.next
artifacts
test-results

# Machine-written daily reports. These arrive from the reporting agent, which commits outside this
# repo's pre-commit hook, so they land unformatted and break `pnpm verify` on main for everyone
# else — five times in two weeks before this line existed. `check:docs` still validates their
# links; only prettier's opinion about their whitespace is dropped.
docs/reports/daily/
