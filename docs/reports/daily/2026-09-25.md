# Daily Report — 2026-09-25

**Prepared by:** Karnain daily-report agent
**Live site:** https://karnain-website.vercel.app
**Live-check method:** curl
**Note:** Notion MCP requires OAuth — report saved to repo only; database write skipped (see Blockers).

---

## Shipped today

Three commits landed on 2026-09-25 — the biggest shipping day since 2026-09-20.

| Commit    | Date       | Type | Subject                                                 |
| --------- | ---------- | ---- | ------------------------------------------------------- |
| `40e6a1c` | 2026-09-25 | docs | docs(specs): mark 014 shipped                           |
| `b70ba7b` | 2026-09-25 | feat | feat(i18n): site in EN, IT, ES, DE (spec 014) (#7)      |
| `39c6175` | 2026-09-25 | fix  | fix(home): apply client visual feedback and repair Instagram link (#6) |

**feat(i18n) — spec 014 shipped** (78 files, +2 807 / −432 lines):
- French stays at bare URLs; English at `/en`, Italian `/it`, Spanish `/es`, German `/de`.
- All public routes moved under `src/app/[locale]/(site)`; locale handed to client components via `I18nProvider`; pages remain statically built per locale.
- `src/proxy.ts` rewrites bare paths to `/fr` and redirects `/fr/*` to bare paths; unknown prefixes 404.
- Product copy (name, notes, mood, description) localised in Supabase via new `translations` jsonb column; two migrations applied to production.
- `formatEur(value, locale)` formats euros per locale; Stripe Checkout receives locale and localized return URLs.
- Titles, `<html lang>`, Open Graph locale, and `hreflang` alternates on every public page.
- E2E cases added per language; ADR 0007, feature doc, CUJ-D, and report 014 committed.
- Non-French copy is AI-drafted and **awaits client review**.
- Next.js bumped to 16.3.6 for stable `root-params` API.

**fix(home) — client visual feedback applied** (#6):
- Instagram links and displayed handle corrected to `@karnain_paris` (previous `/karnain` profile did not exist).
- Four "Suivez-nous" tiles replaced with client's new visuals.
- Full-bleed six-fragrance strip added after the "Histoire" section (title band cropped; header carries brand name).
- Campaign backdrop swapped for sunlit flat lay; band taller on desktop.

---

## Live site

Checked with **curl** (Playwright not run). All five CUJ routes healthy; checkout active.

| Route              | Status | Title                      |
| ------------------ | ------ | -------------------------- |
| `/`                | ✅ 200 | Karnain — Maison de parfum |
| `/collection`      | ✅ 200 | Karnain Addicte · Karnain  |
| `/parfums/tobacco` | ✅ 200 | Tobacco · Karnain          |
| `/panier`          | ✅ 200 | Votre panier · Karnain     |
| `/admin/login`     | ✅ 200 | Admin                      |

**Checkout API:** `/api/checkout` → `{"enabled":true}` — Stripe remains active.

Locale-prefixed routes (`/en`, `/it`, `/es`, `/de`) were deployed as part of spec 014 and are expected live; not checked via curl (would require full path checks per route). No downtime or error responses on the five CUJ paths. JavaScript console errors cannot be inspected via curl.

---

## Progress since last report

Delta vs 2026-09-24 daily report:

- **Spec 014 (multilingual) shipped.** Site now serves French (default), English, Italian, Spanish, and German. This is the first feature shipped since spec 011/012 (checkout + admin settings, 2026-09-20). Previous four reports noted no code activity.
- **Homepage refreshed.** Instagram handle corrected to `@karnain_paris`; new client visuals and fragrance strip applied.
- **No blockers cleared.** SMTP env vars, Notion MCP, Stripe webhook, Legal/CGV, Rose des Bois remain open.
- **New follow-up introduced:** AI-drafted non-French copy (EN/IT/ES/DE product descriptions) awaits client review before the translated pages can be publicly promoted.

---

## In progress

No open spec branch. Spec 014 just marked shipped.

Outstanding items awaiting client input:

- **Non-French copy review** (NEW) — EN/IT/ES/DE product name translations are AI-drafted; client must review/approve before promoting the multilingual URLs.
- Real contact email / phone number (placeholder in `core/site.ts`)
- Instagram integration confirmed at `@karnain_paris` ✅ — handle was the open item from spec 001.
- Rose des Bois bottle photograph (product remains draft)
- Stripe live webhook confirmation (webhook URL against current Vercel deployment unverified)
- Legal/CGV copy (required before checkout can be publicly promoted)
- SMTP environment variables (`SMTP_HOST`, `SMTP_USER`, `SMTP_PASSWORD`, `MAIL_FROM`)

---

## Next

1. **Client review of non-French copy** — share `/en`, `/it`, `/es`, `/de` URLs with the client for approval of AI-drafted product translations.
2. **Configure SMTP env vars in Vercel** — set `SMTP_HOST`, `SMTP_USER`, `SMTP_PASSWORD`, `MAIL_FROM` to activate order email notifications.
3. **Verify Stripe live webhook** — confirm webhook URL in the Stripe dashboard matches `/api/stripe/webhook` on the current Vercel deployment.
4. **Legal/CGV copy** — required before checkout can be publicly promoted.
5. **Rose des Bois photo** — upload via `/admin` once an approved bottle image is provided.
6. **Close stale spec tasks** — tick off completed items in specs 002–004 `tasks.md` (cart, collection, editorial are live; task files not updated).
7. **Domain cutover** — point `karnain.fr` at Vercel when client is ready.
8. **Authorize Notion MCP** — authorize at claude.ai → Settings → Connectors so future reports write to the "Daily Reports" database.

---

## Blockers

- **Non-French copy unreviewed** — EN/IT/ES/DE product descriptions are AI-drafted; must not be publicly promoted until the client approves. **(New today.)**
- **SMTP not configured in Vercel** — order email notification (`feat(orders)`, 2026-09-20) requires `SMTP_HOST`/`SMTP_USER`/`SMTP_PASSWORD`/`MAIL_FROM` in Vercel env. No sale emails will send until set. (Persists from 2026-09-20.)
- **Notion MCP unauthenticated** — OAuth not completed; daily report saved to repo only. **Action:** authorize at claude.ai → Settings → Connectors in an interactive session. (Persists from 2026-09-01.)
- **Stripe webhook registration** — live webhook URL not verified/updated since the 2026-09-07 Supabase project migration. Checkout is `enabled:true` but webhook delivery on the new project is unconfirmed. (Persists from 2026-09-07.)
- **Legal/CGV** — copy pending before public sale can be promoted.
- **Rose des Bois** — draft; no approved bottle photo yet.
- **Stale spec task files** — specs 002–004 `tasks.md` have unchecked items despite features being live; housekeeping only.
