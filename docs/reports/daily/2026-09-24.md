# Daily Report — 2026-09-24

**Prepared by:** Karnain daily-report agent
**Live site:** https://karnain-website.vercel.app
**Live-check method:** curl
**Note:** Notion MCP requires OAuth — report saved to repo only; database write skipped (see Blockers).

---

## Shipped today

No code commits on 2026-09-24. The 3-day window (since 2026-09-21) shows only daily report
commits — no code activity:

| Commit    | Date       | Type | Subject                                    |
| --------- | ---------- | ---- | ------------------------------------------ |
| `624b70e` | 2026-09-23 | docs | docs(reports): add 2026-09-23 daily report |
| `9a49e02` | 2026-09-22 | docs | docs(reports): add 2026-09-22 daily report |
| `dfb2e9a` | 2026-09-21 | docs | docs(reports): add 2026-09-21 daily report |

Most recent code commits remain the three shipped on 2026-09-20 (`feat(orders)` email
notification, `chore` prettierignore fix, `test` Vitest server-only stub).

---

## Live site

Checked with curl. All 5 CUJ routes healthy, no change from yesterday.

| Route              | Status | Title                      |
| ------------------ | ------ | -------------------------- |
| `/`                | ✅ 200 | Karnain — Maison de parfum |
| `/collection`      | ✅ 200 | Karnain Addicte · Karnain  |
| `/parfums/tobacco` | ✅ 200 | Tobacco · Karnain          |
| `/panier`          | ✅ 200 | Votre panier · Karnain     |
| `/admin/login`     | ✅ 200 | Admin · Karnain            |

**Checkout API:** `/api/checkout` → `{"enabled":true}` — Stripe remains active.

No errors, no downtime. JavaScript console errors cannot be inspected via curl; use Playwright
or a browser session to check for runtime JS errors.

---

## Progress since last report

Delta vs 2026-09-23 daily report:

- **No code change.** No commits have landed on 2026-09-24; codebase is identical to the
  2026-09-20 state (four days of report-only commits).
- **Live site unchanged.** All five CUJ routes stable at 200 with expected French titles.
- **Checkout active.** `/api/checkout` still returns `enabled:true`.
- **All persistent blockers remain.** SMTP env vars, Notion MCP, Stripe webhook, Legal/CGV,
  Rose des Bois — none cleared since the last report.
- **No new blockers introduced.**

---

## In progress

No open spec branch. All features (specs 001, 005, 009–012) confirmed live.

Outstanding items awaiting client input:

- Real contact email / phone / Instagram handle (placeholders in `core/site.ts`)
- Rose des Bois bottle photograph (product remains draft; no approved image)
- Stripe live webhook confirmation (webhook URL against current Vercel deployment unverified)
- Legal/CGV copy (required before checkout can be publicly promoted)
- SMTP environment variables (`SMTP_HOST`, `SMTP_USER`, `SMTP_PASSWORD`, `MAIL_FROM`) —
  order emails will not send until these are set in Vercel project settings

---

## Next

1. **Configure SMTP env vars in Vercel** — set `SMTP_HOST`, `SMTP_USER`, `SMTP_PASSWORD`,
   `MAIL_FROM` in Vercel project settings to activate order email notifications.
2. **Verify Stripe live webhook** — confirm webhook URL in the Stripe dashboard matches
   `/api/stripe/webhook` on the current Vercel deployment.
3. **Test a live Stripe purchase** — end-to-end payment with a Stripe live card against
   production.
4. **Legal/CGV copy** — pending before checkout can be publicly promoted.
5. **Close stale spec tasks** — tick off completed items in specs 002–004 `tasks.md`
   (cart, collection, editorial are live; task files not updated).
6. **Rose des Bois photo** — upload via `/admin` once an approved bottle image is provided.
7. **Domain cutover** — point `karnain.fr` at Vercel when client is ready.
8. **Authorize Notion MCP** — authorize at claude.ai → Settings → Connectors so future
   reports write to the "Daily Reports" database.

---

## Blockers

- **SMTP not configured in Vercel** — order email notification (`feat(orders)` shipped
  2026-09-20) requires `SMTP_HOST`/`SMTP_USER`/`SMTP_PASSWORD`/`MAIL_FROM` in Vercel env.
  Until set, no sale emails will be sent. **Action:** add vars in Vercel project settings.
  (Persists from 2026-09-20.)
- **Notion MCP unauthenticated** — OAuth not completed; daily report saved to repo only.
  **Action:** authorize at claude.ai → Settings → Connectors in an interactive session.
  (Persists from 2026-09-01.)
- **Stripe webhook registration** — live webhook URL not verified/updated since the
  2026-09-07 Supabase project migration. Checkout is `enabled:true` but webhook delivery
  on the new project is unconfirmed. (Persists from 2026-09-07.)
- **Legal/CGV** — copy pending before public sale can be promoted.
- **Rose des Bois** — draft; no approved bottle photo yet.
- **Stale spec task files** — specs 002–004 `tasks.md` have unchecked items despite
  features being live; housekeeping only.
